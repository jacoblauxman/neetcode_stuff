// # 238 Product of Array Except Self
// note: `daily` for 3/15/24
// MUST be done in O(n) Time WIHTOUT division operator

// prefix array? postfix array? combine concept into output array (doesn't count towards Big O Space)
// two passes over `nums` input
// default value of `1` for BOTH passes
// start at `beginning` first pass - second pass at `end`

pub fn product_except_self(nums: Vec<i32>) -> Vec<i32> {}
